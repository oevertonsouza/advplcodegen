#include "TOTVS.CH"

Class Col${className} from Collection
	   
    Method New() Constructor

    Method initEntity()
    Method bsc${className}()
    Method commit()
    Method delete()

EndClass

Method New() Class Col${className}
    _Super:new()
    self:oDao := Dao${className}():New()
    self:oMapper := Mpr${className}():New()
return self

Method initEntity() Class Col${className}
return ${className}():New()

Method commit() Class Col${className}
    
    self:lFound := self:getDao():commit()
    If !self:found()
        self:setError("Não conseguiu gravar/alterar o registro na entidade ${entity}.")
    EndIf

return self:found()

Method delete() Class Col${className}
    
    self:lFound := self:getDao():delete()
    If !self:found()
        self:setError("Não conseguiu deletar o registro na entidade ${entity}.")
    EndIf

return self:found()




