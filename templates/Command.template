#INCLUDE "TOTVS.CH"

/*/{Protheus.doc} ${prefix}Com${className}
    Classe abstrata para execução de comandos
    @type  Class
    @author everton.mateus
    @since 20190320
/*/
Class ${prefix}Com${className}
	
    Data oExecutor
    
    Method New(oExecutor) Constructor
    Method execute()
    
EndClass

Method New(oExecutor) Class ${prefix}Com${className}
    self:oExecutor := oExecutor
Return self

Method execute() Class ${prefix}Com${className}
Return nil