#include "TOTVS.CH"
#include "autorizador.ch"

/*/{Protheus.doc} 
    Classe concreta da Entidade $entity
    @type  Class
    @author FrameworkApi 1.0
    @since 20190211
/*/
Class $className from AbstractEntity

    Method New(oDao)
  
$properts
    Method serialize(oJsonControl)
    Method getFields()

    Method destroy()

EndClass

Method New(oDao) Class $className
	_Super:New(oDao)
Return self

$gets 
Method serialize(oJsonControl) Class $className
	local oJson          := JsonObject():New()
	default oJsonControl := AutJsonControl():New()

$serialize
Return oJson

Method getFields() Class $className

	self:oFields := HMList():New()

$fields

Return self:oFields

Method destroy() Class className

	_Super:destroy()
	DelClassIntF()

return